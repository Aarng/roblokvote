# Static Site configuration for Render
# This serves the files directly without needing a Node.js server

staticSites:
  - type: static
    name: roblokvote-static
    buildCommand: echo "No build needed"
    staticPublishPath: .
    envVars:
      - key: VITE_CONVEX_URL
        value: https://hallowed-badger-330.convex.cloud/
